<%= form_for ([review, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
        <% @comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    &ensp;&ensp;<%= f.label :body, "Comment" %>&ensp;
    <%= f.text_area :body, :rows => "1", :cols => "60", placeholder: "Add a Reply" %>
    <%= f.submit "Reply" %>
  </div>
<% end %>
